version: 0.2
env:
  variables:
    JAVA_HOME: "/usr/lib/jvm/java-17-amazon-corretto-devel/"
phases:
  install:
    commands:
      - alternatives --config java
      - yum update; yum -y install java-17-amazon-corretto-devel
      - update-alternatives --auto javac
      - update-alternatives --auto java
  build:
    commands:
      - java -version
      - mvn package
artifacts:
  files:
    - '**/*'